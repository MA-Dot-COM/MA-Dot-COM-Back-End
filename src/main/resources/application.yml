server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://metaverse.ohgiraffers.com:3306/comproject?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: comproject
    password: comproject
    hikari:
      auto-commit: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: true
  data:
    mongodb:
      uri: mongodb+srv://comproject:comproject@comproject.pldtw41.mongodb.net/?retryWrites=true&w=majority
      authentication-database: admin
      database: comproject
  # jpa config
  jpa:
    generate-ddl: false
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
#      ddl-auto: 'create-drop'
#    hibernate:
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: asdfjp298pafo1320efqw0udvnh0er0h412r08byfdq9dsvh02143908hfeqwn80fdqs0nfdqs0n412013rq0nqefn08dfas0das730732g86923

logging:
  level:
    root: info
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error

cloud:
  aws:
    s3:
      bucket: combucket
    region:
      static: ap-northeast-2
    stack:
      auto: false
    credentials:
      instanceProfile: true

url:
  ai: "http://"